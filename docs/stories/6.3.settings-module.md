# Story 6.3: Settings Module — Account & Integration Config

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]

## Story
**As an** operator,
**I want** to configure my account data and integrations in the system,
**so that** the system works correctly with my data and preferences.

## Acceptance Criteria

1. Tela "Settings" com abas: Profile, Integrations, Tracking, Shipping
2. Aba Profile: nome, email (read-only do Facebook), foto, timezone
3. Aba Integrations: status da conexao Facebook (conectado/desconectado), botao reconectar, lista de permissoes atuais, data da ultima sincronizacao de Ad Accounts
4. Aba Tracking (NOVA):
   - Campo "Dominio de Tracking": dominio customizado para links de campanha (ex: `track.brazachat.com`). Default: dominio principal da aplicacao
   - Preview de como o link fica com o dominio configurado: `https://{dominio}/c/ABX92?utm_source=...`
   - Checkbox "Incluir UTMs automaticamente nos links" (ativado por default)
   - Configuracao de UTM padrao: permite ao operador definir `utm_source` padrao (facebook/instagram/custom) e `utm_medium` padrao (paid_social/cpc/custom)
   - Link para politica de privacidade do dominio de tracking (obrigatorio para compliance Meta — campo URL)
   - Indicador de SSL: verificacao automatica se o dominio tem HTTPS valido (verde = OK, vermelho = sem SSL)
5. Aba Shipping: dados do remetente para etiquetas (nome/empresa, endereco, cidade, estado, CEP, telefone)
6. Endpoint `PATCH /users/settings` para salvar configuracoes
7. Dados de remetente usados pela geracao de etiqueta (Story 5.3)
8. Dominio de tracking usado na geracao de links de campanha (Story 2.2)
9. Botao "Desconectar Facebook" com confirmacao (revoga token, mantem dados historicos)
10. Validacao de todos os campos com feedback visual

## Tasks / Subtasks

- [x] Implement `PATCH /users/settings` endpoint
  - [x] Accept profile fields: name, timezone
  - [x] Accept tracking fields: trackingDomain, autoUtm, defaultUtmSource, defaultUtmMedium, privacyPolicyUrl
  - [x] Accept shipping fields: shippingData JSON (name, address, city, state, zipCode, phone)
  - [x] Validate all fields
  - [x] Store settings in User model (shippingData JSON + additional fields)
- [x] Implement Facebook disconnect
  - [x] Revoke token via Meta API
  - [x] Clear accessToken and refreshToken from database
  - [x] Maintain historical data (ad accounts, campaigns, etc.)
- [x] Create frontend Settings page with tabs
  - [x] **Profile tab:** name (editable), email (read-only), photo, timezone dropdown
  - [x] **Integrations tab:** Facebook connection status, reconnect button, permissions list, last sync date
  - [x] **Tracking tab:**
    - [x] Tracking domain input with live link preview
    - [x] "Include UTMs automatically" checkbox
    - [x] Default utm_source and utm_medium configuration
    - [x] Privacy policy URL field
    - [x] SSL indicator (green/red)
  - [x] **Shipping tab:** sender data form (name/company, address, city, state dropdown, CEP, phone)
- [x] Implement SSL check for tracking domain
- [x] "Desconectar Facebook" button with confirmation dialog
- [x] Validate all fields with visual feedback (Zod frontend, class-validator backend)
- [x] Write unit tests (settings update, Facebook disconnect, validation)
- [x] Write frontend component tests

## Dev Notes

### Architecture Reference
- Architecture doc: `docs/architecture.md`
- Design System: `docs/braza-design-system.md`
- PRD: `docs/prd.md`

### Key Technical Details

**API Route:** `PATCH /users/settings` — JWT auth required.

**User Model fields used:**
- `name`, `email` (from Facebook OAuth)
- `timezone` (default: "America/Sao_Paulo")
- `shippingData` (JSON field for sender data used in shipping labels)

**Tracking domain:** Used to construct campaign links in Story 2.2. Default: `link.brazachat.com`.

**Facebook disconnect:** Calls Meta API to revoke token, then clears encrypted tokens from DB. Historical data (campaigns, leads, events) is preserved.

**SSL check:** Can use a simple HTTPS HEAD request to the tracking domain to verify SSL validity.

### Testing
- Unit tests: Jest (packages/api/src/**/*.spec.ts)
- Frontend tests: Jest + React Testing Library (packages/web/src/**/*.test.tsx)
- Follow testing standards from architecture doc

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 0.1.0 | Story created from PRD v0.4.0 | River (SM) |
