# Story 2.2: Campaign Creation with Pixel Dropdown

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]

## Story
**As an** operator,
**I want** to create campaigns linking product, ad account, pixel, and ad data,
**so that** each campaign has a trackable tracking link.

## Acceptance Criteria

1. Endpoints REST: `POST /campaigns`, `GET /campaigns`, `GET /campaigns/:id`, `PATCH /campaigns/:id`
2. Campos: name, productId, adAccountId, pixelId, adsetName, adName, creativeName
3. Dropdown de Ad Account carrega apenas contas do usuario autenticado
4. Dropdown de Pixel filtra automaticamente pelos pixels da Ad Account selecionada (FR8)
5. Dropdown de Product carrega produtos do usuario
6. Ao criar campanha, gera automaticamente um `trackingCode` unico (ex: `ABX92`, 5-6 chars alfanumericos) e cacheia o mapeamento `trackingCode -> campaignData` no Redis (TTL 1h) para performance do redirect
7. Ao criar campanha, gera automaticamente UTM parameters a partir dos dados informados: `utm_source=facebook`, `utm_medium=paid_social`, `utm_campaign={campaign_name}` (slug do nome), `utm_content={creative_name}` (slug do criativo), `utm_term={adset_name}` (slug do adset)
8. Apos criacao, exibir painel "Link de Campanha" com:
   - Link completo montado: `https://{domain}/c/{trackingCode}?utm_source=...&utm_medium=...&utm_campaign=...&utm_content=...&utm_term=...`
   - Tabela visual dos UTMs gerados (parametro | valor) para o operador conferir
   - Botao "Copiar Link Completo" (copia URL inteira com UTMs para clipboard)
   - Botao "Copiar Link Curto" (copia apenas `https://{domain}/c/{trackingCode}` sem UTMs)
   - Preview da mensagem WhatsApp que o lead vai receber ao clicar
   - Botao "Testar Link" que abre o link numa nova aba para o operador verificar o redirect
9. Tela "Campaigns" com listagem em tabela: nome, produto, ad account, link curto (com tooltip mostrando link completo com UTMs ao passar o mouse), total de cliques, taxa de conversao
10. Na listagem, icone de copia rapida ao lado do link (copia link completo com UTMs)
11. Detalhe da campanha (ao clicar): exibe painel completo com link, UTMs, metricas de cliques, e leads gerados
12. UTM parameters editaveis individualmente no detalhe da campanha (campos inline-edit) — ao editar, o link e regenerado automaticamente
13. Validacao: todos os campos obrigatorios, pixelId deve pertencer ao adAccountId selecionado
14. Slug dos UTMs: lowercase, espacos substituidos por hifens, sem caracteres especiais (ex: "Brain Caps V2" -> `brain-caps-v2`)

## Tasks / Subtasks

- [x] Create Campaigns module in backend
  - [x] `campaigns.module.ts`, `campaigns.controller.ts`, `campaigns.service.ts`
  - [x] Create DTOs: `create-campaign.dto.ts`, `update-campaign.dto.ts`
- [x] Implement REST endpoints
  - [x] `POST /campaigns` — create with auto-generated trackingCode and UTMs
  - [x] `GET /campaigns` — list campaigns for user (with click count and conversion rate)
  - [x] `GET /campaigns/:id` — detail view with metrics
  - [x] `PATCH /campaigns/:id` — update campaign (regenerate UTMs if fields change)
- [x] Implement trackingCode generation
  - [x] Generate unique 5-6 char alphanumeric code
  - [x] Cache `trackingCode -> campaignData` in Redis (TTL 1h)
- [x] Implement UTM auto-generation
  - [x] Slug function: lowercase, spaces to hyphens, strip special chars
  - [x] Auto-generate: utm_source=facebook, utm_medium=paid_social, utm_campaign, utm_content, utm_term
- [x] Implement validation
  - [x] All fields required
  - [x] pixelId must belong to selected adAccountId
  - [x] Validate foreign key references (productId, adAccountId, pixelId)
- [x] Create frontend Campaigns page
  - [x] Campaign creation form with cascading dropdowns (Ad Account -> Pixel, Product)
  - [x] Campaign Link panel after creation (full link, short link, UTM table, copy buttons, test link, WhatsApp preview)
  - [x] Campaign listing table with click counts, conversion rates, quick-copy icon
  - [x] Campaign detail view with inline-editable UTMs
  - [x] Tooltip on short link showing full URL with UTMs
- [x] Write unit tests (trackingCode generation, UTM slugification, validation)
- [x] Write frontend component tests

## Dev Notes

### Architecture Reference
- Architecture doc: `docs/architecture.md`
- Design System: `docs/braza-design-system.md`
- PRD: `docs/prd.md`

### Key Technical Details

**Prisma Model:**
```prisma
model Campaign {
  id              String    @id @default(cuid())
  name            String
  trackingCode    String    @unique
  productId       String
  adAccountId     String
  pixelId         String
  adsetName       String
  adName          String
  creativeName    String
  utmSource       String    @default("facebook")
  utmMedium       String    @default("paid_social")
  utmCampaign     String    // slug
  utmContent      String    // slug
  utmTerm         String    // slug
  userId          String
  @@index([userId])
  @@index([trackingCode])
}
```

**Redis caching:** On campaign creation, cache `track:{trackingCode}` -> campaignData in Redis with TTL 1h. This is used by the tracking redirect (Story 2.3) for < 50ms response.

**Link format:** `https://link.brazachat.com/c/{trackingCode}?utm_source=facebook&utm_medium=paid_social&utm_campaign=...&utm_content=...&utm_term=...`

**UTM slug rules:** lowercase, spaces replaced by hyphens, no special characters. "Brain Caps V2" -> `brain-caps-v2`

### Testing
- Unit tests: Jest (packages/api/src/**/*.spec.ts)
- Frontend tests: Jest + React Testing Library (packages/web/src/**/*.test.tsx)
- Follow testing standards from architecture doc

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 0.1.0 | Story created from PRD v0.4.0 | River (SM) |
