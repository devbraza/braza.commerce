# Story 3.2: Send Messages via WhatsApp Cloud API

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]

## Story
**As an** operator,
**I want** to send text messages to leads directly from the CRM,
**so that** I can respond without leaving the system.

## Acceptance Criteria

1. Endpoint `POST /conversations/:id/messages` envia mensagem de texto via WhatsApp Cloud API
2. Mensagem enviada salva no banco com direction `outbound`, status `sent`
3. Webhook de status update (delivered, read) atualiza status da mensagem no banco
4. Se envio falhar (token invalido, numero bloqueado, rate limit), retorna erro descritivo ao frontend
5. Validacao: conversationId deve existir e pertencer ao usuario autenticado
6. Mensagens suportadas no MVP: apenas texto (imagens e midias em epic futuro)
7. Rate limiting por conversa: maximo 30 mensagens/minuto (respeitar limites da Meta)

## Tasks / Subtasks

- [x] Implement `POST /conversations/:id/messages` endpoint
  - [x] Validate conversationId exists and belongs to authenticated user
  - [x] Send text message via WhatsApp Cloud API
  - [x] Save message to DB (direction=outbound, status=sent, whatsappMsgId from API response)
  - [x] Return message data to frontend
- [x] Implement WhatsApp Cloud API send integration
  - [x] HTTP POST to `https://graph.facebook.com/v21.0/{phone_number_id}/messages`
  - [x] Include bearer token from user's decrypted accessToken
  - [x] Handle API error responses (invalid token, blocked number, rate limit)
- [x] Implement message status webhook handler
  - [x] Handle `delivered` status updates
  - [x] Handle `read` status updates
  - [x] Update message status in database
  - [x] Emit Socket.io event for real-time status update in UI
- [x] Implement rate limiting per conversation (30 msg/min)
- [x] Handle error cases with descriptive messages to frontend
- [x] Write unit tests (send flow, status updates, rate limiting, error handling)

## Dev Notes

### Architecture Reference
- Architecture doc: `docs/architecture.md`
- Design System: `docs/braza-design-system.md`
- PRD: `docs/prd.md`

### Key Technical Details

**API Route:** `POST /conversations/:id/messages` — JWT auth required.

**Prisma Model:**
```prisma
model Message {
  id              String    @id @default(cuid())
  conversationId  String
  content         String
  type            String    @default("text")
  direction       String    // inbound, outbound
  status          String    @default("sent") // sent, delivered, read
  whatsappMsgId   String?   // Meta's message ID
  @@index([conversationId])
  @@index([createdAt])
}
```

**WhatsApp Cloud API:** Messages sent via `POST https://graph.facebook.com/v21.0/{phone_number_id}/messages` with bearer token.

**Status updates:** Received via the same webhook endpoint (`POST /webhooks/whatsapp`) — need to distinguish between message notifications and status update notifications.

**Rate limiting:** Per-conversation limit of 30 msgs/min, separate from global API rate limit.

### Testing
- Unit tests: Jest (packages/api/src/**/*.spec.ts)
- Frontend tests: Jest + React Testing Library (packages/web/src/**/*.test.tsx)
- Follow testing standards from architecture doc

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 0.1.0 | Story created from PRD v0.4.0 | River (SM) |
