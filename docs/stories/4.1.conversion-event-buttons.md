# Story 4.1: Conversion Event Buttons in Chat

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]

## Story
**As an** operator,
**I want** AddToCart, InitiateCheckout, and Purchase buttons at the bottom of the chat,
**so that** I can mark the conversion stage with a single click.

## Acceptance Criteria

1. Tres botoes visiveis abaixo do campo de input do chat: AddToCart, InitiateCheckout, Purchase (FR11)
2. Botoes com cores distintas: AddToCart (amarelo), InitiateCheckout (laranja), Purchase (verde)
3. Ao clicar, modal de confirmacao: "Marcar {eventName} para {leadName}?" com botao confirmar
4. Endpoint `POST /conversations/:id/events` cria evento com type, value (opcional) e currency
5. Para Purchase, campo `value` e pre-preenchido com preco do produto e `currency` com moeda do produto
6. Apos confirmar, botao mostra feedback visual e evento aparece como mensagem de sistema no chat
7. Eventos seguem ordem logica: nao permitir Purchase sem InitiateCheckout previo, nem InitiateCheckout sem AddToCart
8. Botao do evento ja registrado fica desabilitado com icone check (nao permite duplicacao)
9. Eventos salvos na tabela Event: leadId, conversationId, type, value, currency, sentToMeta=pending, createdAt

## Tasks / Subtasks

- [x] Create Events module in backend
  - [x] `events.module.ts`, `events.controller.ts`, `events.service.ts`
  - [x] Create DTOs: `create-event.dto.ts`
- [x] Implement `POST /conversations/:id/events` endpoint
  - [x] Validate conversationId belongs to authenticated user
  - [x] Validate event type (AddToCart, InitiateCheckout, Purchase)
  - [x] Enforce event ordering (AddToCart -> InitiateCheckout -> Purchase)
  - [x] For Purchase: use product price/currency as defaults
  - [x] Create Event record with sentToMeta=pending
  - [x] Generate unique eventId for CAPI deduplication
  - [x] Enqueue event in BullMQ `meta-events` queue
- [x] Implement event ordering validation
  - [x] Check existing events for the conversation
  - [x] Block InitiateCheckout if no AddToCart exists
  - [x] Block Purchase if no InitiateCheckout exists
  - [x] Prevent duplicate events of same type
- [x] Create frontend event buttons component
  - [x] Three buttons below chat input: AddToCart (yellow), InitiateCheckout (orange), Purchase (green)
  - [x] Confirmation modal: "Marcar {eventName} para {leadName}?"
  - [x] Purchase modal includes pre-filled value and currency fields
  - [x] Visual feedback after confirmation
  - [x] Disabled state with check icon for already-registered events
  - [x] System message in chat after event creation
- [x] Emit Socket.io event for system message
- [x] Write unit tests (event creation, ordering validation, deduplication)
- [x] Write frontend component tests (buttons, modals, disabled states)

## Dev Notes

### Architecture Reference
- Architecture doc: `docs/architecture.md`
- Design System: `docs/braza-design-system.md`
- PRD: `docs/prd.md`

### Key Technical Details

**Events Module Structure (from architecture doc Section 3.5):**
```
events/
├── events.module.ts
├── events.controller.ts       # POST /conversations/:id/events, GET /events
├── events.service.ts          # Create event, manage status
├── events.processor.ts        # BullMQ processor for Meta CAPI delivery
├── capi/
│   ├── capi.client.ts         # Meta Conversion API HTTP client
│   ├── capi.hasher.ts         # SHA-256 normalization + hashing
│   └── capi.payload.ts        # Build compliant payload
└── dto/
    └── create-event.dto.ts
```

**API Route:** `POST /conversations/:id/events` — JWT auth required.

**Event types and order:** ViewContent (auto) -> AddToCart -> InitiateCheckout -> Purchase

**BullMQ Queue:** Events enqueued to `meta-events` for delivery to Meta CAPI (handled in Story 4.2).

**Connects to Story 5.1:** Purchase event triggers automatic Order creation.

### Testing
- Unit tests: Jest (packages/api/src/**/*.spec.ts)
- Frontend tests: Jest + React Testing Library (packages/web/src/**/*.test.tsx)
- Follow testing standards from architecture doc

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 0.1.0 | Story created from PRD v0.4.0 | River (SM) |
