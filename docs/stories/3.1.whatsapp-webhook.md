# Story 3.1: WhatsApp Cloud API Webhook Setup

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]

## Story
**As a** developer,
**I want** to configure the WhatsApp Cloud API webhook to receive messages in the backend,
**so that** the system captures all messages sent by leads.

## Acceptance Criteria

1. Endpoint `GET /webhooks/whatsapp` para verificacao do webhook (challenge response) conforme documentacao Meta
2. Endpoint `POST /webhooks/whatsapp` recebe notificacoes de mensagens com validacao de assinatura (X-Hub-Signature-256)
3. Webhook processa tipos de mensagem: text, image, audio, document, location
4. Ao receber primeira mensagem de um numero, sistema tenta vincular ao Lead existente via click_id extraido da mensagem
5. Se click_id encontrado na mensagem, associa conversation ao lead/click correspondente
6. Se click_id nao encontrado, cria lead orfao com status `unmatched` para triagem manual
7. Mensagem recebida salva no banco: conversationId, content, type (text/image/etc), direction (inbound), timestamp
8. Logs estruturados para cada webhook recebido (debug de integracao)
9. Retorna HTTP 200 imediatamente e processa mensagem de forma assincrona (fila Redis) para nao bloquear webhook
10. Endpoint protegido contra replay attacks (validacao de timestamp da notificacao)
11. Webhook URL em producao: `https://api.brazachat.com/webhooks/whatsapp` (URL publica estavel no Railway). Em dev: ngrok ou Cloudflare Tunnel
12. Rate limiter independente no endpoint webhook (separado das rotas autenticadas) — protege contra flood

## Tasks / Subtasks

- [x] Create WhatsApp module in backend
  - [x] `whatsapp.module.ts`, `whatsapp.controller.ts`, `whatsapp.service.ts`
  - [x] `whatsapp.webhook.ts` — webhook validation and signature check
  - [x] `whatsapp.processor.ts` — BullMQ processor for async message handling
  - [x] Create DTOs: `webhook-payload.dto.ts`, `send-message.dto.ts`
- [x] Implement `GET /webhooks/whatsapp` (webhook verification)
  - [x] Challenge response per Meta documentation
  - [x] Use WHATSAPP_VERIFY_TOKEN for validation
- [x] Implement `POST /webhooks/whatsapp` (message reception)
  - [x] Validate X-Hub-Signature-256 with WHATSAPP_APP_SECRET
  - [x] Return HTTP 200 immediately
  - [x] Enqueue message in BullMQ `whatsapp-messages` queue
  - [x] Protect against replay attacks (timestamp validation)
- [x] Implement BullMQ message processor
  - [x] Extract click_id from message text
  - [x] If click_id found: match to existing Lead/Click, create Conversation
  - [x] If click_id not found: create orphan Lead with status `unmatched`
  - [x] Save Message to DB (conversationId, content, type, direction=inbound)
  - [x] Emit Socket.io event to operator's room
  - [x] If first message: dispatch ViewContent event (connects to Story 3.5)
- [x] Handle message types: text, image, audio, document, location
- [x] Implement structured logging for webhook events
- [x] Configure independent rate limiter for webhook endpoint
- [x] Configure BullMQ queue `whatsapp-messages` (concurrency: 10, retry: 3x with 5s/30s/2min backoff)
- [x] Write unit tests (signature validation, click_id extraction, lead matching, message processing)

## Dev Notes

### Architecture Reference
- Architecture doc: `docs/architecture.md`
- Design System: `docs/braza-design-system.md`
- PRD: `docs/prd.md`

### Key Technical Details

**WhatsApp Module Structure (from architecture doc Section 3.3):**
```
whatsapp/
├── whatsapp.module.ts
├── whatsapp.controller.ts     # GET/POST /webhooks/whatsapp (PUBLIC)
├── whatsapp.service.ts        # Send messages via Cloud API
├── whatsapp.webhook.ts        # Webhook validation, signature check
├── whatsapp.processor.ts      # BullMQ processor for async message handling
└── dto/
    ├── webhook-payload.dto.ts
    └── send-message.dto.ts
```

**Webhook Flow (from architecture doc):**
```
POST /webhooks/whatsapp
  -> Validate X-Hub-Signature-256
  -> Return 200 immediately
  -> Enqueue message in BullMQ "whatsapp-messages"
  -> Processor:
    -> Extract click_id from message text
    -> Match to Lead (or create orphan)
    -> Save Message to DB
    -> Emit Socket.io event to operator's room
    -> If first message -> dispatch ViewContent event
```

**BullMQ Queue Config:** `whatsapp-messages` — concurrency 10, retry 3x (5s, 30s, 2min).

**Environment Variables:** `WHATSAPP_TOKEN`, `WHATSAPP_VERIFY_TOKEN`, `WHATSAPP_APP_SECRET`

### Testing
- Unit tests: Jest (packages/api/src/**/*.spec.ts)
- Frontend tests: Jest + React Testing Library (packages/web/src/**/*.test.tsx)
- Follow testing standards from architecture doc

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 0.1.0 | Story created from PRD v0.4.0 | River (SM) |
