# Story 4.3: Events Log & Monitoring

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]

## Story
**As an** operator,
**I want** to see the history of all events sent to Meta,
**so that** I can audit conversions and identify delivery failures.

## Acceptance Criteria

1. Endpoint `GET /events` com filtros: campaignId, type, sentToMeta, dateRange
2. Tela "Events" no frontend com tabela: lead, produto, campanha, tipo de evento, valor, status de envio, data
3. Badge de status colorido: sent (verde), pending (amarelo), failed (vermelho), skipped (cinza)
4. Ao clicar num evento, drawer/modal mostra detalhes completos: payload enviado, resposta da Meta, horario de cada tentativa
5. Contador no topo da tela: total de eventos, taxa de sucesso (%), eventos com falha
6. Botao "Reenviar" para eventos com status `failed` — reenfileira no BullMQ
7. Paginacao com 50 eventos por pagina
8. Dados de eventos tambem acessiveis via endpoint `GET /leads/:id/events`

## Tasks / Subtasks

- [x] Implement `GET /events` endpoint
  - [x] Support filters: campaignId, type, sentToMeta, dateRange
  - [x] Include related data: lead name/phone, product name, campaign name
  - [x] Pagination: 50 events per page
  - [x] Return summary counters: total, success rate, failed count
- [x] Implement `GET /leads/:id/events` endpoint
  - [x] Return events for a specific lead
- [x] Implement resend functionality
  - [x] Endpoint to re-enqueue failed events in BullMQ
  - [x] Reset retryCount and sentToMeta to pending
- [x] Create frontend Events page
  - [x] Table: lead, product, campaign, event type, value, send status, date
  - [x] Status badges: sent (green), pending (yellow), failed (red), skipped (gray)
  - [x] Click event to open detail drawer/modal
  - [x] Detail view: full payload, Meta response, retry timestamps
  - [x] Top counters: total events, success rate (%), failed events
  - [x] "Reenviar" button for failed events
  - [x] Pagination (50 per page)
  - [x] Filters: campaign, event type, send status, date range
- [x] Write unit tests (listing, filtering, resend logic)
- [x] Write frontend component tests

## Dev Notes

### Architecture Reference
- Architecture doc: `docs/architecture.md`
- Design System: `docs/braza-design-system.md`
- PRD: `docs/prd.md`

### Key Technical Details

**API Routes:**
- `GET /events` — JWT auth, filtered by TenantGuard
- `GET /leads/:id/events` — JWT auth

**Event statuses:** `pending`, `sent`, `failed`, `skipped`

**Event detail should include:**
- Full CAPI payload that was sent to Meta
- Meta API response (stored in `metaResponse` JSON field)
- Retry history: retryCount, lastRetryAt
- eventMatchQuality score (from Meta response)

**Resend flow:** Reset `sentToMeta` to `pending`, `retryCount` to 0, re-enqueue in BullMQ `meta-events` queue.

### Testing
- Unit tests: Jest (packages/api/src/**/*.spec.ts)
- Frontend tests: Jest + React Testing Library (packages/web/src/**/*.test.tsx)
- Follow testing standards from architecture doc

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 0.1.0 | Story created from PRD v0.4.0 | River (SM) |
