# Story 1.4: Facebook OAuth Authentication

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]

## Story
**As a** user,
**I want** to log into BrazaChat using my Facebook account,
**so that** I can securely access the system and connect my ad accounts.

## Acceptance Criteria

1. Botao "Conectar com Facebook" na tela de login do frontend
2. Fluxo OAuth completo: redirect para Facebook -> autorizacao -> callback -> criacao de sessao JWT
3. Permissoes solicitadas: `ads_management`, `ads_read`, `business_management`, `whatsapp_business_management`
4. Ao logar pela primeira vez, cria registro `User` com facebookId, email e name
5. Ao logar novamente, atualiza accessToken e refreshToken do usuario existente
6. AccessToken e refreshToken criptografados com `CryptoService` (AES-256-GCM) antes de persistir no banco — nunca armazenados em plaintext
7. Token JWT armazenado em httpOnly cookie com expiracao de 7 dias
8. Endpoint `GET /auth/me` retorna dados do usuario autenticado (sem expor tokens Meta)
9. Rotas protegidas passam pelo `TenantGuard` que valida JWT e injeta userId no request — retornam 401 se invalido ou expirado
10. Frontend redireciona para `/dashboard` apos login bem-sucedido
11. Frontend redireciona para `/login` se acessar rota protegida sem autenticacao

## Tasks / Subtasks

- [x] Create Auth module structure in backend
  - [x] `auth.module.ts`, `auth.controller.ts`, `auth.service.ts`
  - [x] Create `strategies/facebook.strategy.ts` (Passport Facebook Strategy)
  - [x] Create `guards/jwt-auth.guard.ts`
  - [x] Create `dto/auth-response.dto.ts`
- [x] Implement Facebook OAuth flow
  - [x] `POST /auth/facebook` — initiates OAuth redirect to Facebook
  - [x] `GET /auth/callback` — handles OAuth callback from Facebook
  - [x] Request permissions: `ads_management`, `ads_read`, `business_management`, `whatsapp_business_management`
- [x] Implement user creation/update logic
  - [x] First login: create User with facebookId, email, name
  - [x] Subsequent login: update accessToken and refreshToken
  - [x] Encrypt tokens with CryptoService before persisting (never store plaintext)
- [x] Implement JWT session management
  - [x] Generate JWT token on successful OAuth
  - [x] Store JWT in httpOnly cookie (7 days expiration, SameSite=Lax)
  - [x] `GET /auth/me` returns user data without Meta tokens
- [x] Implement JwtAuthGuard
  - [x] Validate JWT from httpOnly cookie
  - [x] Return 401 for invalid/expired tokens
- [x] Integrate TenantGuard globally for authenticated routes
- [x] Create frontend login page
  - [x] "Conectar com Facebook" button with Facebook branding
  - [x] Redirect to `/dashboard` after successful login
  - [x] Redirect to `/login` when accessing protected routes without auth
- [x] Write unit tests for auth service (OAuth flow, token encryption, JWT generation)
- [x] Write unit tests for JWT guard and tenant guard

## Dev Notes

### Architecture Reference
- Architecture doc: `docs/architecture.md`
- Design System: `docs/braza-design-system.md`
- PRD: `docs/prd.md`

### Key Technical Details

**Auth Module Structure (from architecture doc Section 3.1):**
```
auth/
├── auth.module.ts
├── auth.controller.ts        # /auth/facebook, /auth/callback, /auth/me
├── auth.service.ts            # OAuth flow, JWT generation
├── strategies/
│   └── facebook.strategy.ts   # Passport Facebook Strategy
├── guards/
│   ├── jwt-auth.guard.ts      # Validates JWT from httpOnly cookie
│   └── tenant.guard.ts        # Injects userId (global)
└── dto/
    └── auth-response.dto.ts
```

**Security layers (from architecture doc Section 7):**
- Layer 2: Facebook OAuth via Passport.js
- Layer 2: JWT in httpOnly cookie (7 days, SameSite=Lax)
- Layer 3: TenantGuard userId injection
- Layer 4: CryptoService AES-256-GCM for Meta tokens

**Environment variables needed:** `FACEBOOK_APP_ID`, `FACEBOOK_APP_SECRET`, `JWT_SECRET`, `ENCRYPTION_KEY`

**API Routes:**
- `POST /auth/facebook` — Start OAuth (no auth)
- `GET /auth/callback` — OAuth callback (no auth)
- `GET /auth/me` — Current user (JWT required)

### Testing
- Unit tests: Jest (packages/api/src/**/*.spec.ts)
- Frontend tests: Jest + React Testing Library (packages/web/src/**/*.test.tsx)
- Follow testing standards from architecture doc

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 0.1.0 | Story created from PRD v0.4.0 | River (SM) |
