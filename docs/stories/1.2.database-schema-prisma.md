# Story 1.2: Database Schema & Prisma Setup

## Status
Ready for Review

## Executor Assignment
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: ["lint", "typecheck", "test"]

## Story
**As a** developer,
**I want** Prisma configured with PostgreSQL and base models created,
**so that** the backend has functional data persistence from the start.

## Acceptance Criteria

1. Prisma inicializado em `packages/api` com datasource PostgreSQL (connection string via `DATABASE_URL`)
2. Schema contém os modelos: `User`, `AdAccount`, `Pixel`, `Product`, `Campaign`, `Click`, `Lead`, `Conversation`, `Message`, `Event`, `Order`, `AiInsight`
3. Relacionamentos implementados conforme hierarquia: AdAccount -> Pixel -> Campaign -> Lead -> Conversation -> Event -> Order
4. Modelo `Click` contém campos adicionais de compliance: fbc (formato `fb.1.ts.fbclid`), fbp (formato `fb.1.ts.random`), utm_source, utm_medium, utm_campaign, utm_content, utm_term
5. Modelo `User` contém campos: id, email, name, facebookId, accessToken (encrypted), refreshToken, createdAt, updatedAt
6. Migration inicial criada e aplicável com `npx prisma migrate dev`
7. Prisma Client gerado e injetável como NestJS service (`PrismaService`)
8. Seed script básico com ao menos 1 user de teste
9. Types do Prisma exportados via `packages/shared` para uso no frontend

## Tasks / Subtasks

- [x] Initialize Prisma in `packages/api` with PostgreSQL datasource
  - [x] Install `prisma` and `@prisma/client`
  - [x] Configure `prisma/schema.prisma` with `DATABASE_URL` env (via prisma.config.ts — Prisma 7)
- [x] Create all 12 Prisma models with exact fields from architecture doc
  - [x] `User` model (id, facebookId, email, name, accessToken, refreshToken, timezone, shippingData, timestamps)
  - [x] `AdAccount` model (id, metaId, name, status, userId, relation to User)
  - [x] `Pixel` model (id, metaId, name, adAccountId, relation to AdAccount)
  - [x] `Product` model (id, name, price, currency, whatsappPhone, messageTemplate, userId)
  - [x] `Campaign` model (id, name, trackingCode, productId, adAccountId, pixelId, adsetName, adName, creativeName, UTM fields, userId)
  - [x] `Click` model (id, clickId, campaignId, fbclid, fbc, fbp, ip, userAgent, UTM fields)
  - [x] `Lead` model (id, phone, name, campaignId, productId, clickId, fbclid, status, userId)
  - [x] `Conversation` model (id, leadId, userId, lastMessageAt, unreadCount)
  - [x] `Message` model (id, conversationId, content, type, direction, status, whatsappMsgId)
  - [x] `Event` model (id, eventId, leadId, conversationId, type, value, currency, sentToMeta, metaResponse, retryCount, lastRetryAt, userId)
  - [x] `Order` model (id, orderId, leadId, conversationId, productId, value, currency, status, address fields, trackingCode, userId)
  - [x] `AiInsight` model (id, type, data, productId, campaignId, confidence, sampleSize, userId)
- [x] Configure all relationships and indexes as defined in architecture doc
  - [x] `@@index` on userId for multi-tenant performance
  - [x] `@@index` on trackingCode (Campaign), clickId (Click), phone (Lead), conversationId (Message), sentToMeta (Event), status (Order)
  - [x] `@unique` constraints on facebookId (User), trackingCode (Campaign), clickId (Click), eventId (Event), orderId (Order)
- [ ] Create initial migration with `npx prisma migrate dev` (requires running PostgreSQL)
- [x] Create `PrismaService` as NestJS injectable service
  - [x] Implement `onModuleInit` for connection
  - [x] Implement `onModuleDestroy` for graceful shutdown
- [x] Create seed script (`prisma/seed.ts`) with test user
- [x] Export Prisma generated types via `packages/shared`
- [ ] Verify migration applies cleanly on fresh database (requires running PostgreSQL)

## Dev Notes

### Architecture Reference
- Architecture doc: `docs/architecture.md`
- Design System: `docs/braza-design-system.md`
- PRD: `docs/prd.md`

### Key Technical Details

**Full Prisma schema is documented in architecture doc Section 4.** All 12 models with exact field types, defaults, relations, and indexes are specified there.

**Key compliance fields on Click model:**
- `fbc`: format `fb.1.{timestamp_unix_ms}.{fbclid}` (Meta CAPI requirement)
- `fbp`: format `fb.1.{timestamp_unix_ms}.{random_10_digits}` (Meta CAPI requirement)
- All UTM parameters stored for attribution tracking

**PrismaService pattern:**
```typescript
@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
  async onModuleInit() { await this.$connect(); }
  async onModuleDestroy() { await this.$disconnect(); }
}
```

**Multi-tenant Prisma middleware** (from architecture doc Section 2.2): automatic filtering by userId on findMany/findFirst queries.

**Database:** PostgreSQL on Supabase (managed) with connection pooling via PgBouncer.

### Testing
- Unit tests: Jest (packages/api/src/**/*.spec.ts)
- Frontend tests: Jest + React Testing Library (packages/web/src/**/*.test.tsx)
- Follow testing standards from architecture doc

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 0.1.0 | Story created from PRD v0.4.0 | River (SM) |
