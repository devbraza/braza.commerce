# Story 1.3: CI/CD Pipeline & GitHub Actions

## Status
Approved

## Executor Assignment
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]

## Story
**As a** developer,
**I want** a CI/CD pipeline that validates code on every push,
**so that** errors are detected before reaching the main branch.

## Acceptance Criteria

1. GitHub Actions workflow (`.github/workflows/ci.yml`) dispara em push e PR para `main`
2. Pipeline executa em sequência: install -> lint -> typecheck -> test -> build
3. Pipeline roda para ambos os packages (api e web)
4. Cache de `node_modules` configurado para acelerar builds subsequentes
5. Pipeline falha se qualquer step falhar (lint errors, type errors, test failures, build errors)
6. Badge de status do CI visível no README.md
7. Tempo total do pipeline abaixo de 5 minutos

## Tasks / Subtasks

- [ ] Create `.github/workflows/ci.yml` workflow file
  - [ ] Configure trigger on push and PR to `main` branch
  - [ ] Set up Node.js environment (Node 18+)
- [ ] Configure npm cache for `node_modules` to speed up builds
  - [ ] Use `actions/cache` with npm cache directory
- [ ] Add pipeline steps in sequence:
  - [ ] Install dependencies (`npm ci`)
  - [ ] Run lint (`npm run lint`)
  - [ ] Run typecheck (`npm run typecheck`)
  - [ ] Run tests (`npm run test`)
  - [ ] Run build (`npm run build`)
- [ ] Ensure pipeline covers both `packages/api` and `packages/web`
- [ ] Configure pipeline to fail-fast on any step failure
- [ ] Add CI status badge to README.md
- [ ] Optimize pipeline to run under 5 minutes
  - [ ] Consider parallel jobs for api and web if needed
- [ ] Test pipeline with a sample PR

## Dev Notes

### Architecture Reference
- Architecture doc: `docs/architecture.md`
- Design System: `docs/braza-design-system.md`
- PRD: `docs/prd.md`

### Key Technical Details

**Workflow structure:**
```yaml
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test
      - run: npm run build
```

**Required tools:** Node.js 18+, npm workspaces (monorepo), GitHub CLI for PR integration.

**Deploy targets (future):** Frontend on Vercel (`app.brazachat.com`), Backend on Railway (`api.brazachat.com`).

### Testing
- Unit tests: Jest (packages/api/src/**/*.spec.ts)
- Frontend tests: Jest + React Testing Library (packages/web/src/**/*.test.tsx)
- Follow testing standards from architecture doc

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 0.1.0 | Story created from PRD v0.4.0 | River (SM) |
