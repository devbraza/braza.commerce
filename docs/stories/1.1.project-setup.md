# Story 1.1: Project Setup & Monorepo Structure

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]

## Story
**As a** developer,
**I want** a configured monorepo with backend (NestJS), frontend (Next.js), and shared package,
**so that** the entire team has an organized and functional codebase from day one.

## Acceptance Criteria

1. Monorepo inicializado com npm workspaces contendo `packages/api`, `packages/web` e `packages/shared`
2. `packages/api` contém NestJS app funcional com health-check endpoint (`GET /health` retorna `{ status: "ok" }`)
3. `packages/web` contém Next.js app com App Router, TailwindCSS e shadcn/ui configurados, exibindo página inicial com texto "BrazaChat"
4. `packages/shared` exporta ao menos um type TypeScript importável por api e web
5. Scripts no root `package.json`: `dev`, `build`, `lint`, `test`, `typecheck` funcionando para ambos os packages
6. ESLint + Prettier configurados com regras consistentes entre api e web
7. `.env.example` atualizado com variáveis necessárias para ambos os packages (incluindo `ENCRYPTION_KEY` para criptografia de tokens)
8. `npm run dev` no root inicia api (porta 3001) e web (porta 3000) simultaneamente
9. `CryptoService` implementado no backend com AES-256-GCM para encrypt/decrypt de tokens Meta (usado pelo auth module)
10. `TenantGuard` implementado como NestJS Guard global que injeta `userId` do JWT em toda request autenticada, garantindo isolamento multi-tenant

## Tasks / Subtasks

- [x] Initialize root `package.json` with npm workspaces configuration (`packages/api`, `packages/web`, `packages/shared`)
- [x] Scaffold `packages/api` with NestJS 11 + TypeScript
  - [x] Configure NestJS app module
  - [x] Create health-check controller (`GET /health` returns `{ status: "ok" }`)
  - [x] Configure Pino logger (nestjs-pino)
  - [x] Configure global exception filter
- [x] Scaffold `packages/web` with Next.js 15 + App Router + React 19
  - [x] Configure TailwindCSS 4
  - [x] Install and configure shadcn/ui
  - [x] Install Lucide React icons
  - [x] Create initial page displaying "BrazaChat"
- [x] Create `packages/shared` with TypeScript types
  - [x] Export at least one shared type (e.g., `ApiResponse<T>`)
  - [x] Configure `tsconfig.json` for shared package
  - [x] Verify import works from both api and web
- [x] Configure root scripts in `package.json`: `dev`, `build`, `lint`, `test`, `typecheck`
  - [x] `npm run dev` starts api (port 3001) and web (port 3000) simultaneously
- [x] Configure ESLint + Prettier with consistent rules across packages
- [x] Create `.env.example` with all required variables including `ENCRYPTION_KEY`
- [x] Implement `CryptoService` in backend
  - [x] AES-256-GCM encrypt method (returns `iv:authTag:ciphertext` base64)
  - [x] AES-256-GCM decrypt method
  - [x] Unit tests for encrypt/decrypt round-trip
- [x] Implement `TenantGuard` as global NestJS Guard
  - [x] Extract userId from JWT payload
  - [x] Inject tenantId into request object
  - [x] Return 401 for invalid/missing JWT
  - [x] Unit tests for TenantGuard
- [x] Verify all scripts work: `npm run dev`, `npm run build`, `npm run lint`, `npm run test`, `npm run typecheck`

## Dev Notes

### Architecture Reference
- Architecture doc: `docs/architecture.md`
- Design System: `docs/braza-design-system.md`
- PRD: `docs/prd.md`

### Key Technical Details

**Monorepo Structure:**
```
packages/
  api/      # NestJS 11, TypeScript, Prisma 6, BullMQ, Socket.io
  web/      # Next.js 15, App Router, React 19, TailwindCSS 4, shadcn/ui, Lucide
  shared/   # Shared TypeScript types
```

**CryptoService Pattern (from architecture doc):**
```typescript
// packages/api/src/common/services/crypto.service.ts
@Injectable()
export class CryptoService {
  private readonly algorithm = 'aes-256-gcm';
  private readonly key: Buffer; // from ENCRYPTION_KEY env var
  encrypt(plaintext: string): string { /* IV random 16 bytes + AES-256-GCM */ }
  decrypt(encrypted: string): string { /* Parse iv:authTag:ciphertext */ }
}
```

**TenantGuard Pattern (from architecture doc):**
```typescript
// packages/api/src/common/guards/tenant.guard.ts
@Injectable()
export class TenantGuard implements CanActivate {
  canActivate(context: ExecutionContext): boolean {
    const request = context.switchToHttp().getRequest();
    const user = request.user; // from JWT
    request.tenantId = user.id;
    return true;
  }
}
```

**Environment Variables (from architecture doc):**
- `DATABASE_URL`, `FACEBOOK_APP_ID`, `FACEBOOK_APP_SECRET`, `JWT_SECRET`, `ENCRYPTION_KEY` (32 bytes hex), `WHATSAPP_TOKEN`, `WHATSAPP_VERIFY_TOKEN`, `REDIS_URL`, `NEXT_PUBLIC_API_URL`, `NEXT_PUBLIC_WS_URL`, `SENTRY_DSN`, `NODE_ENV`

**Domains:** `app.brazachat.com` (frontend), `api.brazachat.com` (backend), `link.brazachat.com` (tracking)

### Testing
- Unit tests: Jest (packages/api/src/**/*.spec.ts)
- Frontend tests: Jest + React Testing Library (packages/web/src/**/*.test.tsx)
- Follow testing standards from architecture doc

## File List
| File | Action | Description |
|------|--------|-------------|
| `package.json` | Created | Root monorepo config with npm workspaces |
| `.prettierrc` | Created | Prettier config (shared rules) |
| `packages/shared/package.json` | Created | Shared package config |
| `packages/shared/tsconfig.json` | Created | Shared TypeScript config |
| `packages/shared/eslint.config.mjs` | Created | Shared ESLint config |
| `packages/shared/src/index.ts` | Created | Shared barrel export |
| `packages/shared/src/types/api-response.ts` | Created | ApiResponse<T> type |
| `packages/api/package.json` | Created | API package config (NestJS 11) |
| `packages/api/tsconfig.json` | Created | API TypeScript config |
| `packages/api/nest-cli.json` | Created | NestJS CLI config |
| `packages/api/eslint.config.mjs` | Created | API ESLint config |
| `packages/api/jest.config.ts` | Created | API Jest config |
| `packages/api/src/main.ts` | Created | NestJS bootstrap (port 3001) |
| `packages/api/src/app.module.ts` | Created | Root app module with Pino logger |
| `packages/api/src/health/health.module.ts` | Created | Health module |
| `packages/api/src/health/health.controller.ts` | Created | GET /health endpoint |
| `packages/api/src/health/health.controller.spec.ts` | Created | Health controller tests |
| `packages/api/src/common/services/crypto.service.ts` | Created | AES-256-GCM encrypt/decrypt |
| `packages/api/src/common/services/crypto.service.spec.ts` | Created | CryptoService tests (6 tests) |
| `packages/api/src/common/guards/tenant.guard.ts` | Created | Multi-tenant guard (JWT userId) |
| `packages/api/src/common/guards/tenant.guard.spec.ts` | Created | TenantGuard tests (3 tests) |
| `packages/api/src/common/filters/global-exception.filter.ts` | Created | Global exception filter |
| `packages/web/package.json` | Created | Web package config (Next.js 15) |
| `packages/web/tsconfig.json` | Created | Web TypeScript config |
| `packages/web/next.config.ts` | Created | Next.js config (transpile shared) |
| `packages/web/postcss.config.mjs` | Created | PostCSS with TailwindCSS 4 |
| `packages/web/eslint.config.mjs` | Created | Web ESLint config |
| `packages/web/jest.config.ts` | Created | Web Jest config |
| `packages/web/src/app/globals.css` | Created | TailwindCSS imports |
| `packages/web/src/app/layout.tsx` | Created | Root layout (pt-BR) |
| `packages/web/src/app/page.tsx` | Created | Home page with "BrazaChat" |
| `packages/web/src/lib/utils.ts` | Created | cn() utility (shadcn/ui) |
| `.env.example` | Modified | Added BrazaChat app variables |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### Debug Log References
No issues encountered.

### Completion Notes
- All 10 acceptance criteria implemented
- 10 unit tests passing (3 suites: CryptoService, TenantGuard, HealthController)
- `npm run build`, `npm run lint`, `npm run test`, `npm run typecheck` all passing
- shadcn/ui configured with cn() utility, class-variance-authority, clsx, tailwind-merge, and Lucide icons
- CryptoService uses AES-256-GCM with random IV, returns `iv:authTag:ciphertext` in base64

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 0.1.0 | Story created from PRD v0.4.0 | River (SM) |
| 2026-03-15 | 0.2.0 | All tasks implemented — monorepo, API, web, shared, CryptoService, TenantGuard | Dex (Dev) |
